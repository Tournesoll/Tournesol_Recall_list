# Recall Lite 本周开发布置

大家这次统一从 GitHub `main` 的基线 commit 开分支，不直接改 `main`，完成后提 PR。开发前请先阅读 `skill_开发前必看/recall-lite-team-development/SKILL.md` 及自己的角色文档；所有人都必须遵守文件所有权，不要修改分工外的文件，不要提交 API Key、真实学习资料、备份或临时测试文件。

本周日（具体日期以群内通知为准）前完成：提交 GitHub PR，或把可运行的 Web/Android 包和开发后说明发到群里。建议尽早提交阶段性结果，不要等最后一天才暴露冲突。

## 袁翊博

负责项目基线、跨模块接线、Bug 修复、最终合并和 Android 导出。当前已经预置好 `src/domain.ts` 和 `src/contracts.ts` 两个共享合同：大家直接复用，不要重复定义同名类型。等贾源和候舒扬完成后，我按“贾源 → 候舒扬 → 共享接线 → 回归 → Android”的顺序合并。

## 候舒扬

只修改 `src/features/ai/**` 和自己的开发后说明，负责 MiMo v2.5 统一客户端、API Key 本地配置、文本/TXT/图片 AI 导入、题目解析选项、预览编辑、最多 3 次 JSON 自动修复、背诵中当前卡片临时问 AI、记录页 7/30 天学习分析。不要修改 `App.tsx`、`db.ts`、全局 CSS、`package.json` 或 Android；需要接线的地方在说明文档中写清楚。

## 贾源

负责 UI 和非 AI 功能，可修改 `src/App.tsx`、`src/db.ts`、`src/styles.css`、`src/v2.css`、`src/features/details/**`、`src/features/study-tasks/**` 和自己的开发后说明。重点完成 360 px 手机端适配、长文本不横向溢出、统一“题目解析/笔记”详细介绍区域，以及最多 3 个未完成背诵任务的 IndexedDB 保存、暂停、继续、新建、删除和完成历史。直接复用 `src/domain.ts`，不要实现 MiMo 请求，也不要修改 `src/features/ai/**`。

## 杨桀浩

只负责 `开发报告.md` 和自己的文档工作说明，依据实际 PR、commit、测试和开发后说明写报告。不要把计划写成已完成，不要修改代码。等两位开发者和袁翊博的合并说明齐全后，补全最终功能、架构、测试、APK 和已知限制。

## 统一提交要求

- PR 标题说明类型和范围，例如 `feat(ai): add MiMo import preview`。
- PR 描述必须列出修改文件、数据结构、测试命令、手工验证、已知问题和需要袁翊博接入的位置。
- 每人提交前填写 `开发后产出文档/` 下自己的说明。
- 临时 mock、测试路由、测试数据、调试日志和真实密钥测试完必须删除。
- 最低测试：`npm run build`、360×800 手机宽度、长文本、空/失败状态和自己负责功能的核心流程。
